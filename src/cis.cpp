#include <iostream>
#include <fstream>

// @Zedritsch

const char REPLACEMENTS[] = {'\t', '\n', ' '};
const char BUILT_IN[] = {'"', '$', '\'', '(', ')', '*', ',', '.', ':', ';', '?', '@', '[', ']', '{', '}'};


int main(const int ARGC, const char *ARGV[]) {
	if (ARGC < 2)
		return 0;

	return 1;
}